server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # 反向代理 ClickHouse HTTP 接口，规避浏览器跨域限制
    location /clickhouse/ {
        proxy_pass http://clickhouse:8123/;
    }
}
